# 🧮 Calculadora de IMC

## Descrição

Este projeto é uma **Calculadora de IMC (Índice de Massa Corporal)** desenvolvida em **HTML, CSS e JavaScript**.  
Ele permite que o usuário informe seu peso e altura e receba o valor do IMC junto com a classificação correspondente (magreza, normal, sobrepeso, obesidade, obesidade grave), com mudança dinâmica de cores conforme o resultado da categoria e tabela de classificações.

## Tecnologias Utilizadas

- **HTML5** – Estrutura da página  
- **CSS3** – Estilização visual  
- **JavaScript** – Lógica de cálculo do IMC

## Estrutura do Repositório

- `index.html` – Página principal com o formulário de entrada  
- `css/` – Pasta com arquivo(s) de estilo CSS  
- `js/` – Pasta com arquivo(s) JavaScript

## Como Usar

1. Clone o repositório:

   ```bash
   git clone https://github.com/eduardaperez/projeto-calculadora-imc.git
   
2. Acesse o diretório do projeto:

   ```bash
    cd projeto-calculadora-imc
   
3. Abra o arquivo index.html em seu navegador favorito.

4. Insira seu peso e altura nos campos indicados e veja o resultado do cálculo do IMC.

## Como funciona o cálculo

O IMC é calculado utilizando a fórmula:

  **IMC = peso / (altura × altura)**

O resultado é classificado automaticamente de acordo com as faixas padrão de IMC.
